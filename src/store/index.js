import Vue from 'vue';
import Vuex from 'vuex';

import uploadImageModule from './uploadImageModule';

Vue.use(Vuex);

export default new Vuex.Store({
  modules: {
    uploadImageModule,
  },
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
});
